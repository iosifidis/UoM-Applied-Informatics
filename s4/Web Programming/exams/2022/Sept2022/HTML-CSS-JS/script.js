let picture = document.getElementById('picture');

function changePictureWidth() {
    picture.style.width = picture.clientWidth + 10 + 'px'; 
}

function changePictureHeight() {
    picture.style.height = picture.clientHeight + 10 + 'px';
}